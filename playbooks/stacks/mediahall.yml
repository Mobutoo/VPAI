---
# playbooks/stacks/mediahall.yml — Stack MediaHall DCIM NetBox
# Séparé de site.yml javisi — environnement client isolé
# Serveur cible : Story Engine existant (headscale-node déjà installé — absent de ce playbook)
# Usage: ansible-playbook playbooks/stacks/mediahall.yml --ask-vault-pass

- hosts: mediahall
  become: true
  roles:
    - role: common
      tags: [common, mediahall, core]
    - role: docker
      tags: [docker, mediahall, core]
    - role: netbox
      tags: [netbox, mediahall, apps]
    - role: caddy
      tags: [caddy, mediahall, platform]
