# Caddyfile — {{ sd_project_name }} reverse proxy
# Managed by Ansible — DO NOT EDIT MANUALLY
# TLS via HTTP-01 challenge (automatic HTTPS)
# Domains: see domain-strategy.md

{
    email infra@{{ sd_domain }}
}

# --- {{ sd_zammad_fqdn }} — Zammad (ticketing) ---
{{ sd_zammad_fqdn }} {
    reverse_proxy zammad-nginx:8080
}

# --- {{ sd_discourse_fqdn }} — Discourse (KB / community) ---
{{ sd_discourse_fqdn }} {
    reverse_proxy discourse:3000
}

# --- {{ sd_gatus_fqdn }} — Gatus (status page) ---
{{ sd_gatus_fqdn }} {
    reverse_proxy gatus:8080
}

# --- {{ sd_portail_fqdn }} — Next.js client dashboard ---
{{ sd_portail_fqdn }} {
    reverse_proxy portail:3000
}
