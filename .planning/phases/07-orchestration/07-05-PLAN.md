---
phase: 07-orchestration
plan: 05
type: execute
wave: 3
depends_on: ["07-04"]
files_modified: []
autonomous: false
requirements: [FLOW-01, FLOW-02, FLOW-03, FLOW-04, FLOW-05, FLOW-06, FLOW-07, CAL-01, CAL-02, CAL-03]
must_haves:
  truths:
    - "All 8 CF workflows are imported and active in n8n on Sese-AI"
    - "Each workflow webhook responds to POST requests (no 404/500)"
    - "Kitsu credentials work (cf-kitsu-sync can authenticate)"
    - "Plane calendar sync creates work items"
  artifacts: []
  key_links:
    - from: "n8n workflows on Sese-AI"
      to: "All CF webhook endpoints"
      via: "HTTPS at mayi.ewutelo.cloud/webhook/cf-*"
      pattern: "cf-brief-to-concept|cf-kitsu-sync|cf-calendar-sync"
---

<objective>
Deploy all Phase 7 workflows to Sese-AI via Ansible and verify they are active and responding.

Purpose: This is the integration gate -- workflows exist as files but have not been deployed. This plan runs the actual deployment and verifies the live system works.
Output: All 8 CF workflows live on Sese-AI, verified via smoke tests.
</objective>

<execution_context>
@/home/mobuone/.claude/get-shit-done/workflows/execute-plan.md
@/home/mobuone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-orchestration/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy n8n-provision to Sese-AI</name>
  <files></files>
  <action>
Run Ansible deployment targeting the n8n-provision role to deploy all new workflows.

**Step 1 -- Dry run first:**
```bash
source .venv/bin/activate
ansible-playbook playbooks/site.yml --check --diff --tags n8n-provision
```
Verify the dry run shows expected changes: new workflow files copied, checksums updated.

**Step 2 -- Deploy for real:**
```bash
make deploy-role ROLE=n8n-provision ENV=prod
```

**Step 3 -- Also deploy n8n role (for env var changes):**
Since we modified `n8n.env.j2`, the n8n container needs recreation to pick up new env vars (KITSU_*, PLANE_API_KEY).
```bash
make deploy-role ROLE=n8n ENV=prod
```

**IMPORTANT:** The n8n handler must use `state: present` + `recreate: always` (not `state: restarted`) to reload env_file. This is already configured per Phase 06 decision.

**Step 4 -- Verify workflows imported:**
```bash
ssh -i ~/.ssh/seko-vpn-deploy -p 804 mobuone@137.74.114.167 \
  "docker exec javisi_n8n n8n list:workflow" | grep -i "CF "
```
Should show all 8 CF workflows: CF Brief to Concept, CF Script to Storyboard, CF Generate Assets, CF Rough Cut, CF Invalidation Engine, CF Kitsu Sync, CF Calendar Sync.

**Step 5 -- Verify env vars loaded:**
```bash
ssh -i ~/.ssh/seko-vpn-deploy -p 804 mobuone@137.74.114.167 \
  "docker inspect javisi_n8n | python3 -c \"import sys,json; [print(e) for e in json.load(sys.stdin)[0]['Config']['Env'] if 'KITSU' in e or 'PLANE_API' in e]\""
```
Should show KITSU_BOT_EMAIL, KITSU_BOT_PASSWORD, KITSU_API_URL, and PLANE_API_KEY (if vault var exists).
  </action>
  <verify>
    <automated>COUNT=$(ssh -i ~/.ssh/seko-vpn-deploy -p 804 mobuone@137.74.114.167 "docker exec javisi_n8n n8n list:workflow 2>/dev/null | grep -c 'CF '") && [ "$COUNT" -eq 8 ] && echo "OK: 8 CF workflows deployed (7 + inbound)"</automated>
  </verify>
  <done>8 CF workflows imported and active in n8n, Kitsu env vars loaded in container</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify deployed workflows respond</name>
  <action>Human verifies all 8 CF workflows are active in n8n UI and webhook endpoints respond to POST requests.</action>
  <what-built>All 7 Content Factory workflows deployed to Sese-AI n8n instance. Workflows handle: brief-to-concept (steps 1-5), script-to-storyboard (steps 6-8), generate-assets (per-scene dispatch), rough-cut (Remotion assembly), invalidation-engine (cascade invalidation), kitsu-sync (production tracking), calendar-sync (Plane editorial calendar).</what-built>
  <how-to-verify>
1. Open n8n at https://mayi.ewutelo.cloud and verify all 8 CF workflows are visible and active (green toggle)
2. Test cf-brief-to-concept webhook responds (does not need to succeed fully -- just not 404):
   ```
   curl -s -o /dev/null -w "%{http_code}" -X POST https://mayi.ewutelo.cloud/webhook/cf-brief-to-concept \
     -H 'Content-Type: application/json' \
     -d '{"secret":"test","content_id":"1","brand_id":"1","title":"test","format":"reel-motion-text","brief":{"objective":"test"}}'
   ```
   Expected: 200 or 500 (workflow runs but may fail on missing data -- NOT 404 which means webhook not registered)
3. Test cf-kitsu-sync can authenticate with Kitsu:
   ```
   curl -s -X POST https://mayi.ewutelo.cloud/webhook/cf-kitsu-sync \
     -H 'Content-Type: application/json' \
     -d '{"secret":"<actual_secret>","action":"update_task_status","content_id":"1","data":{"step":1,"status":"wip"}}'
   ```
   Check n8n execution log for auth success
4. Verify Plane calendar sync endpoint exists:
   ```
   curl -s -o /dev/null -w "%{http_code}" -X POST https://mayi.ewutelo.cloud/webhook/cf-calendar-sync \
     -H 'Content-Type: application/json' \
     -d '{"secret":"test","action":"sync_content","data":{}}'
   ```
5. Check Kitsu env vars are loaded: in n8n, create a test workflow with a Code node that returns `$env.KITSU_BOT_EMAIL` -- should return `javisi.bot@gmail.com`
  </how-to-verify>
  <verify>Human confirms all 8 workflows active and endpoints respond</verify>
  <done>All CF webhook endpoints respond, Kitsu auth works, deployment approved</done>
  <resume-signal>Type "approved" if all 8 workflows are active and webhook endpoints respond, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All 8 CF workflows visible in n8n UI with green (active) toggles
- Webhook endpoints respond (not 404)
- Kitsu authentication works from n8n
- Plane API key available (if configured in vault)
</verification>

<success_criteria>
- 8 CF workflows deployed and active on Sese-AI
- Each cf-* webhook endpoint responds to POST (no 404)
- KITSU_BOT_EMAIL env var accessible from n8n Code nodes
- Human confirms deployment looks correct
</success_criteria>

<output>
After completion, create `.planning/phases/07-orchestration/07-05-SUMMARY.md`
</output>
