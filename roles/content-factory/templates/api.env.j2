# {{ ansible_managed }}
# Content Factory API (apps/api — FastAPI, app.main:app, port 8000)
# Shared Postgres password (CLAUDE.md): always postgresql_password.
CF_DATABASE_URL=postgresql+psycopg://{{ cf_db_user }}:{{ postgresql_password }}@cf-postgres:5432/{{ cf_db_name }}
