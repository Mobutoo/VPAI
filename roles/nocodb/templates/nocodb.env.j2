# {{ ansible_managed }}
NC_DB=pg://postgresql:5432?u=nocodb&p={{ postgresql_password }}&d=nocodb
NC_PUBLIC_URL=https://{{ nocodb_subdomain }}.{{ domain_name }}
NC_AUTH_JWT_SECRET={{ nocodb_jwt_secret }}
NC_TOOL_DIR=/data
NC_DISABLE_TELE=true

# Stockage S3 — Hetzner Object Storage (fsn1)
# Les pièces jointes des tables NocoDB sont uploadées dans {{ nocodb_s3_bucket }}.
# NC_S3_FORCE_PATH_STYLE=true obligatoire pour les providers S3-compatibles (non-AWS).
NC_S3_BUCKET_NAME={{ nocodb_s3_bucket }}
NC_S3_REGION={{ s3_region }}
NC_S3_ENDPOINT=https://{{ s3_endpoint }}
NC_S3_ACCESS_KEY={{ s3_access_key }}
NC_S3_ACCESS_SECRET={{ s3_secret_key }}
NC_S3_FORCE_PATH_STYLE=true

# NC_API_TOKEN : à provisionner manuellement après le premier démarrage.
# Étapes : NocoDB UI → Team & Auth → API Tokens → créer un token
# Puis stocker dans vault_nocodb_api_token et redéployer :
#   make deploy-role ROLE=nocodb ENV=prod  (pour propager dans openclaw.env + n8n.env)
