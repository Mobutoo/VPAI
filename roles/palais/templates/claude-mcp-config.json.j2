{
  "palais": {
    "type": "sse",
    "url": "https://{{ palais_subdomain }}.{{ domain_name }}/api/mcp/sse",
    "headers": {
      "X-API-Key": "{{ palais_api_key }}"
    },
    "description": "Palais v2 — Infrastructure Control Plane (fleet, workspaces, deploy, costs, domains, memory)"
  }
}
