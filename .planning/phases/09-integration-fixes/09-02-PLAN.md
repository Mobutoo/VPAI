---
phase: 09-integration-fixes
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - roles/kitsu-provision/templates/provision-kitsu.sh.j2
autonomous: false
requirements: [FLOW-07]
gap_closure: true

must_haves:
  truths:
    - "Kitsu bot account javisi.bot@gmail.com exists in Zou and can authenticate"
    - "Kitsu project Paul Taff exists with Production/Episode/Sequence hierarchy"
    - "All template fixes from Plan 01 are deployed and active on Sese-AI"
    - "Kitsu events reach n8n cf-kitsu-inbound webhook"
  artifacts:
    - path: "roles/kitsu-provision/templates/provision-kitsu.sh.j2"
      provides: "Idempotent bot account creation"
      contains: "vault_kitsu_bot_email"
  key_links:
    - from: "roles/kitsu-provision/templates/provision-kitsu.sh.j2"
      to: "inventory/group_vars/all/secrets.yml"
      via: "Jinja2 variables vault_kitsu_bot_email, vault_kitsu_bot_password"
      pattern: "vault_kitsu_bot"
    - from: "roles/kitsu/templates/event_handler.py.j2"
      to: "n8n cf-kitsu-inbound webhook"
      via: "HTTP POST with HMAC signature"
      pattern: "n8n_webhook_secret"
---

<objective>
Add bot account provisioning to Kitsu, deploy all fixes to Sese-AI, and verify the 3 E2E flows work.

Purpose: Plan 01 fixed the template/workflow code. This plan ensures the runtime state matches -- bot account exists, project exists, and all deployed services use the corrected code.
Output: Fully deployed and verified integration fixes on Sese-AI.
</objective>

<execution_context>
@/home/mobuone/.claude/get-shit-done/workflows/execute-plan.md
@/home/mobuone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-integration-fixes/09-RESEARCH.md
@.planning/phases/09-integration-fixes/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bot account creation to Kitsu provisioning script</name>
  <files>roles/kitsu-provision/templates/provision-kitsu.sh.j2</files>
  <read_first>
    - roles/kitsu-provision/templates/provision-kitsu.sh.j2
    - roles/kitsu-provision/tasks/main.yml
  </read_first>
  <action>
In `roles/kitsu-provision/templates/provision-kitsu.sh.j2`, add a bot account creation block BEFORE the sentinel file check (i.e., before the `if [ -f ... .provision-complete ]` guard). This ensures the bot creation runs every time, even if the sentinel exists from Phase 5 provisioning.

Add the following block after the admin account creation section (which also runs before sentinel):

```bash
# === Bot account (idempotent — zou create-admin warns if exists) ===
echo "[$(date)] Creating bot account..."
docker exec "${CONTAINER}" /opt/zou/env/bin/zou create-admin \
  "{{ vault_kitsu_bot_email }}" --password "{{ vault_kitsu_bot_password }}" 2>&1 || {
  echo "[$(date)] WARNING: Bot account creation failed (may already exist)"
}
echo "[$(date)] Bot account ready."
```

Use the same pattern as the existing admin creation: `docker exec ${CONTAINER} /opt/zou/env/bin/zou create-admin`. The `CONTAINER` variable is already defined at the top of the script. `zou create-admin` is idempotent -- it creates a superuser if not exists or warns if it already exists.

The vault variables `vault_kitsu_bot_email` (`javisi.bot@gmail.com`) and `vault_kitsu_bot_password` must already exist in secrets.yml (handled by Plan 01 Task 3 checkpoint).
  </action>
  <verify>
    <automated>cd /home/mobuone/VPAI && grep -c 'vault_kitsu_bot_email' roles/kitsu-provision/templates/provision-kitsu.sh.j2 && grep -c 'vault_kitsu_bot_password' roles/kitsu-provision/templates/provision-kitsu.sh.j2</automated>
  </verify>
  <acceptance_criteria>
    - provision-kitsu.sh.j2 contains `{{ vault_kitsu_bot_email }}`
    - provision-kitsu.sh.j2 contains `{{ vault_kitsu_bot_password }}`
    - Bot creation block appears BEFORE the sentinel check line (`.provision-complete`)
    - Bot creation uses `zou create-admin` command (same pattern as admin creation)
  </acceptance_criteria>
  <done>SC-6 resolved: Bot account creation is in the provisioning script and will run on next deploy</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy to Sese-AI and verify all 8 success criteria</name>
  <files>none (deploy and verify only)</files>
  <action>
Deploy all 4 roles (kitsu, kitsu-provision, n8n, n8n-provision) to Sese-AI via Ansible and run verification commands for all 8 success criteria. Use Tailscale IP 100.64.0.14 port 804 for SSH access (public IP may timeout).

Deploy commands:
```bash
cd /home/mobuone/VPAI && source .venv/bin/activate
make deploy-role ROLE=kitsu ENV=prod
make deploy-role ROLE=kitsu-provision ENV=prod
make deploy-role ROLE=n8n ENV=prod
make deploy-role ROLE=n8n-provision ENV=prod
```
  </action>
  <verify>
    <automated>cd /home/mobuone/VPAI && source .venv/bin/activate && ansible-playbook playbooks/site.yml --check --diff -t kitsu,n8n 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Deploy commands complete without errors
    - SC-1: rendered event_handler.py on server shows non-empty WEBHOOK_SECRET
    - SC-3: n8n container env shows REMOTION_API_KEY and BYTEPLUS_API_KEY
    - SC-6: bot account login returns success
    - SC-7: Paul Taff project exists in Kitsu API response
  </acceptance_criteria>
  <done>All 8 success criteria verified on Sese-AI: Kitsu events reach n8n, Remotion API calls correct, env vars present, bot account active, project exists, vault credentials match</done>
</task>

</tasks>

<verification>
All 8 SCs verified on production:
1. event_handler uses `n8n_webhook_secret` (deployed, rendered correctly)
2. creative-pipeline calls `/renders` with `compositionId`, reads `jobId`
3. n8n env has `REMOTION_API_KEY` + `BYTEPLUS_API_KEY`
4. cf-rough-cut sends `action: 'update_content'`
5. STATUS_MAP includes `locked -> Approved`
6. Bot account `javisi.bot@gmail.com` can authenticate
7. Paul Taff project exists in Kitsu
8. Vault admin email is `seko.mobutoo@gmail.com`
</verification>

<success_criteria>
- Ansible deploy completes without errors for kitsu, kitsu-provision, n8n, n8n-provision roles
- Bot account authenticates via Kitsu API
- Paul Taff project visible in Kitsu API
- n8n container environment includes REMOTION_API_KEY and BYTEPLUS_API_KEY
- Event handler on deployed Kitsu instance uses correct webhook secret variable
</success_criteria>

<output>
After completion, create `.planning/phases/09-integration-fixes/09-02-SUMMARY.md`
</output>
