---
phase: 08-data-layer-glue
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - roles/n8n-provision/tasks/main.yml
autonomous: false
requirements: [DATA-06]
gap_closure: true

must_haves:
  truths:
    - "4 new CF CRUD workflows are registered in Ansible deploy loop and deploy to n8n on Sese-AI"
    - "All 4 webhook endpoints respond to POST requests after deployment"
    - "Kitsu project structure exists on Sese-AI with production/episode/sequence hierarchy (DATA-06)"
  artifacts:
    - path: "roles/n8n-provision/tasks/main.yml"
      provides: "4 new workflow entries in copy + checksum + import + activate loops"
      contains: "cf-create-content"
  key_links:
    - from: "roles/n8n-provision/tasks/main.yml"
      to: "roles/n8n-provision/files/workflows/cf-*.json"
      via: "Ansible copy + checksum loop"
      pattern: "cf-create-content"
    - from: "n8n webhooks"
      to: "NocoDB API"
      via: "HTTP requests inside Code nodes"
      pattern: "NOCODB_BASE_URL"
---

<objective>
Register the 4 new NocoDB CRUD webhook workflows in the Ansible n8n-provision deploy loop, deploy to Sese-AI, and verify both webhook endpoints and Kitsu project structure (DATA-06).

Purpose: Completes the Ansible integration so `make deploy-role ROLE=n8n-provision` deploys all 4 new webhooks. Also formally verifies DATA-06 which was built in Phase 5 but never confirmed.
Output: Updated Ansible tasks + successful deploy + DATA-06 verified
</objective>

<execution_context>
@/home/mobuone/.claude/get-shit-done/workflows/execute-plan.md
@/home/mobuone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-data-layer-glue/08-01-SUMMARY.md
@.planning/phases/08-data-layer-glue/08-RESEARCH.md

<interfaces>
<!-- From roles/n8n-provision/tasks/main.yml — 4 loops that need 4 new entries each -->

Loop 1: "Copy n8n workflow files to server" (ansible.builtin.copy)
  Add to loop list:
    - cf-create-content
    - cf-read-content
    - cf-update-content
    - cf-scene

Loop 2: "Check workflow update status" (checksum comparison)
  Add to loop list:
    - {file: "cf-create-content", name: "CF Create Content"}
    - {file: "cf-read-content", name: "CF Read Content"}
    - {file: "cf-update-content", name: "CF Update Content"}
    - {file: "cf-scene", name: "CF Scene"}

Loop 3: "Store workflow checksums" (post-deploy)
  Add to loop list:
    - {file: "cf-create-content", name: "CF Create Content"}
    - {file: "cf-read-content", name: "CF Read Content"}
    - {file: "cf-update-content", name: "CF Update Content"}
    - {file: "cf-scene", name: "CF Scene"}

Loop 4: "Clean up temporary workflow files"
  Add to loop list:
    - cf-create-content
    - cf-read-content
    - cf-update-content
    - cf-scene

<!-- SSH access to Sese-AI -->
ssh -i ~/.ssh/seko-vpn-deploy -p 804 mobuone@137.74.114.167

<!-- Deploy command -->
source /home/mobuone/VPAI/.venv/bin/activate && cd /home/mobuone/VPAI && make deploy-role ROLE=n8n-provision ENV=prod
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 4 CRUD workflows in Ansible deploy loops and deploy</name>
  <files>roles/n8n-provision/tasks/main.yml</files>
  <read_first>
    roles/n8n-provision/tasks/main.yml,
    roles/n8n-provision/files/workflows/cf-create-content.json,
    roles/n8n-provision/files/workflows/cf-scene.json
  </read_first>
  <action>
Edit `roles/n8n-provision/tasks/main.yml` to add 4 new workflow entries to ALL 4 loops that process workflow files:

**Loop 1: "Copy n8n workflow files to server"** (the `ansible.builtin.copy` task around line 273)
Add these 4 entries at the end of the loop list (after `cf-kitsu-inbound`):
```yaml
    - cf-create-content
    - cf-read-content
    - cf-update-content
    - cf-scene
```

**Loop 2: "Check workflow update status"** (the checksum comparison `ansible.builtin.shell` task around line 317)
Add these 4 entries at the end of the loop list (after the `cf-kitsu-inbound` entry):
```yaml
    - {file: "cf-create-content", name: "CF Create Content"}
    - {file: "cf-read-content", name: "CF Read Content"}
    - {file: "cf-update-content", name: "CF Update Content"}
    - {file: "cf-scene", name: "CF Scene"}
```

**Loop 3: "Store workflow checksums after successful deploy"** (around line 531)
Add these 4 entries at the end of the loop list (after the `cf-kitsu-inbound` entry):
```yaml
    - {file: "cf-create-content", name: "CF Create Content"}
    - {file: "cf-read-content", name: "CF Read Content"}
    - {file: "cf-update-content", name: "CF Update Content"}
    - {file: "cf-scene", name: "CF Scene"}
```

**Loop 4: "Clean up temporary workflow files"** (around line 597)
Add these 4 entries at the end of the loop list (after `cf-kitsu-inbound`):
```yaml
    - cf-create-content
    - cf-read-content
    - cf-update-content
    - cf-scene
```

After editing, run lint and deploy:
```bash
source /home/mobuone/VPAI/.venv/bin/activate
cd /home/mobuone/VPAI
make lint
make deploy-role ROLE=n8n-provision ENV=prod
```

The deploy will: copy JSON files to server, checksum compare, docker cp into n8n container, import via `n8n import:workflow`, publish via `n8n publish:workflow`, store checksums, restart n8n.
  </action>
  <verify>
    <automated>cd /home/mobuone/VPAI && grep -c 'cf-create-content\|cf-read-content\|cf-update-content\|cf-scene' roles/n8n-provision/tasks/main.yml</automated>
  </verify>
  <acceptance_criteria>
    - roles/n8n-provision/tasks/main.yml contains `cf-create-content` in copy loop
    - roles/n8n-provision/tasks/main.yml contains `cf-read-content` in checksum loop
    - roles/n8n-provision/tasks/main.yml contains `cf-update-content` in checksum loop
    - roles/n8n-provision/tasks/main.yml contains `cf-scene` in copy loop
    - roles/n8n-provision/tasks/main.yml contains `"CF Create Content"` in checksum loop
    - roles/n8n-provision/tasks/main.yml contains `"CF Scene"` in checksum loop
    - `source .venv/bin/activate && make lint` exits 0
    - Deploy output shows `IMPORTED: CF Create Content`
    - Deploy output shows `ACTIVATED: CF Create Content`
  </acceptance_criteria>
  <done>All 4 CRUD workflows registered in all 4 Ansible loops and successfully deployed to Sese-AI</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify webhook endpoints and DATA-06 on Sese-AI</name>
  <files>N/A</files>
  <action>
Human verifies that all 4 NocoDB CRUD webhook workflows are active on Sese-AI and that the Kitsu project structure (DATA-06) exists. The executor runs the verification commands below and presents results to the user for approval.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/seko-vpn-deploy -p 804 mobuone@137.74.114.167 "docker exec javisi_n8n n8n list:workflow 2>/dev/null | grep -ci 'CF '" | grep -q '[4-9]' && echo "OK: 4+ CF workflows found"</automated>
  </verify>
  <what-built>4 NocoDB CRUD webhook workflows deployed to n8n on Sese-AI, plus Kitsu project structure from Phase 5</what-built>
  <how-to-verify>
    1. SSH to Sese-AI and verify all 4 webhooks are registered:
       ```
       ssh -i ~/.ssh/seko-vpn-deploy -p 804 mobuone@137.74.114.167 \
         "docker exec javisi_n8n n8n list:workflow 2>/dev/null | grep -i 'CF '"
       ```
       Expected: CF Create Content, CF Read Content, CF Update Content, CF Scene all appear

    2. Verify Kitsu provisioning sentinel (DATA-06):
       ```
       ssh -i ~/.ssh/seko-vpn-deploy -p 804 mobuone@137.74.114.167 \
         "cat /opt/javisi/configs/kitsu/.provision-complete 2>/dev/null && echo 'SENTINEL_EXISTS' || echo 'SENTINEL_MISSING'"
       ```
       Expected: SENTINEL_EXISTS

    3. Verify Kitsu project structure via API (DATA-06):
       ```
       ssh -i ~/.ssh/seko-vpn-deploy -p 804 mobuone@137.74.114.167 \
         "docker exec javisi_kitsu curl -sf http://localhost/api/data/projects 2>/dev/null | python3 -c 'import sys,json; data=json.load(sys.stdin); [print(p[\"name\"]) for p in data]'"
       ```
       Expected: "Paul Taff" appears in output

    4. Type "approved" if all checks pass, or describe issues
  </how-to-verify>
  <acceptance_criteria>
    - n8n list:workflow output includes "CF Create Content"
    - n8n list:workflow output includes "CF Read Content"
    - n8n list:workflow output includes "CF Update Content"
    - n8n list:workflow output includes "CF Scene"
    - Kitsu sentinel file exists at /opt/javisi/configs/kitsu/.provision-complete
    - Kitsu API returns "Paul Taff" project name
  </acceptance_criteria>
  <done>All 4 CRUD webhooks active on Sese-AI and DATA-06 formally verified</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All 4 CRUD webhook workflows appear in `n8n list:workflow` output on Sese-AI
- Kitsu provisioning sentinel file exists at `/opt/javisi/configs/kitsu/.provision-complete`
- Kitsu API returns "Paul Taff" project
- `make lint` passes
</verification>

<success_criteria>
- 4 new workflow entries in all 4 Ansible loops (16 additions total)
- `make deploy-role ROLE=n8n-provision ENV=prod` succeeds
- All 4 webhooks listed and active in n8n on Sese-AI
- DATA-06 formally verified (sentinel + API)
</success_criteria>

<output>
After completion, create `.planning/phases/08-data-layer-glue/08-02-SUMMARY.md`
</output>
