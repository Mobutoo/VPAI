---
# qdrant — handlers

- name: Restart qdrant stack
  community.docker.docker_compose_v2:
    project_src: "/opt/{{ project_name }}"
    files:
      - docker-compose.yml
    services:
      - qdrant
    state: restarted
  become: true
