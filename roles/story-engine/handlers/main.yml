---
# story-engine — handlers

- name: Recreate story-engine stack
  community.docker.docker_compose_v2:
    project_src: "{{ story_engine_base_dir }}"
    build: always
    state: present
    recreate: always
  become: true
  when:
    - not ansible_check_mode
