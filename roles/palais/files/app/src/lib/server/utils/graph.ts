/**
 * Cycle detection utility — task dependencies removed in v2.
 * Kept as stub to avoid breaking imports elsewhere.
 */
export async function hasCycle(_taskId: number, _dependsOnId: number): Promise<boolean> {
	return false;
}
