---
profile: production

# Strict mode
strict: true

# Rules config
enable_list:
  - fqcn
  - no-changed-when
  - no-handler
  - yaml

skip_list:
  - galaxy[no-changelog]        # Pas de changelog pour les rôles internes
  - var-naming[no-role-prefix]  # monitoring role gère plusieurs services (Loki, Grafana, VictoriaMetrics, Alloy)
                                # — variables préfixées par service par design (loki_*, grafana_*, etc.)

warn_list:
  - experimental
  - role-name              # Noms de rôles avec tirets (convention projet)

# Exclude
exclude_paths:
  - .github/
  - .venv/
  - collections/
  - molecule/
  - FS/
  - scripts/

# Offline mode (pas de téléchargement Galaxy pendant le lint)
offline: true

# Extra vars pour que syntax-check fonctionne sans inventaire complet
extra_vars:
  project_display_name: "VPAI"
  target_env: "prod"

# Coloring
use_default_rules: true

# Kinds mapping
kinds:
  - playbook: "playbooks/*.yml"
  - tasks: "roles/*/tasks/*.yml"
  - handlers: "roles/*/handlers/*.yml"
  - vars: "roles/*/vars/*.yml"
  - defaults: "roles/*/defaults/*.yml"
  - meta: "roles/*/meta/*.yml"
