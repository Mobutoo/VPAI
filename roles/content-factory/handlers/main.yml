---
# content-factory — handlers (variante Sese-AI)

- name: Recreate content-factory stack
  community.docker.docker_compose_v2:
    project_src: "{{ cf_config_dir }}"
    build: always
    state: present
    recreate: always
  become: true
  when:
    - not ansible_check_mode
