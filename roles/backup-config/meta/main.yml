---
galaxy_info:
  role_name: backup_config
  author: VPAI
  description: Backup configuration — pre-backup scripts, cron, Zerobyte integration
  license: MIT
  min_ansible_version: "2.16"
  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie

dependencies:
  - role: docker
