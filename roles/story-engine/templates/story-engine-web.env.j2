# {{ ansible_managed }}
# StoryEngine Web (Next.js) — runtime env
# Note: NEXT_PUBLIC_* vars are baked at build time via docker build args.
# These are only for server-side runtime if needed.
NODE_ENV=production
