# Continue Here — Phase 9: Integration Fixes

## Status

**Wave 1 (09-01): COMPLETE**
- Task 1: Fixed event_handler variable + added REMOTION_API_KEY/BYTEPLUS_API_KEY to n8n.env.j2 (commit 245a7f7)
- Task 2: Fixed creative-pipeline /renders + compositionId + jobId, cf-rough-cut action field, STATUS_MAP locked (commit 7b114c2)
- Task 3: Updated vault kitsu_admin_email to seko.mobutoo@gmail.com (commit 5971673)
- SUMMARY: commit baff198

**Wave 2 (09-02): IN PROGRESS — checkpoint reached**
- Task 1: Added bot account creation to provision-kitsu.sh.j2 (commit 1a44b45)
- Task 2: PENDING — Deploy to Sese-AI + verify 8 SCs

## What to Do Next

1. Deploy all fixes to Sese-AI:
   ```bash
   source .venv/bin/activate
   make deploy-role ROLE=kitsu ENV=prod
   make deploy-role ROLE=kitsu-provision ENV=prod
   make deploy-role ROLE=n8n ENV=prod
   make deploy-role ROLE=n8n-provision ENV=prod
   ```

2. Verify the 8 success criteria (SC-1 through SC-8) on Sese-AI

3. Resume execution:
   - Run `/gsd:execute-phase 9` — it will detect 09-01 SUMMARY exists, skip it, and resume 09-02
   - Or spawn a continuation agent for 09-02 Task 2 with user_response = "approved"

4. After 09-02 completes: verifier runs, then `/gsd:complete-milestone`

## Key Context

- SSH to Sese-AI uses Tailscale IP: `100.64.0.14:804` (public IP times out)
- Kitsu admin: seko.mobutoo@gmail.com / mysecretpassword
- Bot: javisi.bot@gmail.com (password in vault)
- All template/workflow fixes are committed but NOT yet deployed
