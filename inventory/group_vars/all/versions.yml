---
# inventory/group_vars/all/versions.yml — Images Docker pinnées
# JAMAIS de :latest ou :stable — Tout est versionné
# Vérifié le : 16 avril 2026

# --- MediaHall — NetBox DCIM ---
netbox_image: "ghcr.io/netbox-community/netbox:v4.2.9"
# Image custom buildée avec plugin netbox-topology-views==4.2.1
# Dockerfile-Plugins: FROM netbox_image, RUN pip install netbox-topology-views==4.2.1
netbox_local_image: "mediahall-netbox:v4.2.9"

# --- Données ---
postgresql_image: "postgres:18.3-bookworm"  # CVE-2025-8714/8715 + buffer overflows (fix 18.3, 2026-02-26)
redis_image: "redis:8.4.0-bookworm"  # CVE-2025-49844 RediShell CVSS 10.0 (fix 8.2.2+, 2025-10)
qdrant_image: "qdrant/qdrant:v1.17.1"

# --- Applications ---
openclaw_image: "ghcr.io/openclaw/openclaw:2026.4.14"
# n8n: image custom avec patch enterprise (roles/n8n/files/Dockerfile)
# Build: cd roles/n8n/files && docker build --build-arg N8N_VERSION=2.7.3 -t ghcr.io/mobutoo/n8n-enterprise:2.7.3 .
# Push:  docker push ghcr.io/mobutoo/n8n-enterprise:2.7.3
n8n_image: "ghcr.io/mobutoo/n8n-enterprise:2.7.3"
n8n_upstream_version: "2.7.3"  # version officielle n8nio/n8n sous-jacente
litellm_image: "ghcr.io/berriai/litellm:v1.83.3-stable"
nocodb_image: "nocodb/nocodb:2026.04.1"  # SQL injection DATEADD + stored XSS + SSRF (fix 0.301.3, 2026-02-27); CalVer depuis 2026.04.1
plane_web_image: "makeplane/plane-frontend:v1.3.0"  # fix SSRF webhook + X-Frame-Options (2026-04)
plane_admin_image: "makeplane/plane-admin:v1.3.0"
plane_api_image: "makeplane/plane-backend:v1.3.0"
plane_worker_image: "makeplane/plane-backend:v1.3.0"
plane_minio_image: "minio/minio:RELEASE.2024-11-07T00-52-20Z"
dufs_image: "sigoden/dufs:v0.45.0"

# --- Reverse Proxy ---
caddy_image: "caddy:2.11.2-alpine"  # CVE-2026-30851/30852 identity injection + env leak (fix 2.11.2)

# --- Observabilité ---
grafana_image: "grafana/grafana:12.4.3"  # CVE-2026-27876 CVSS 9.1 RCE SQL expressions (fix 12.4.2); patch 12.4.3 (2026-04)
victoriametrics_image: "victoriametrics/victoria-metrics:v1.140.0"
loki_image: "grafana/loki:3.7.1"
alloy_image: "grafana/alloy:v1.15.1"

# --- Monitoring (collecteurs) ---
cadvisor_image: "ghcr.io/google/cadvisor:v0.56.2"  # fix nil pointer deref Docker

# --- Système ---
diun_image: "crazymax/diun:4.31.0"
socket_proxy_image: "tecnativa/docker-socket-proxy:v0.4.2"

# --- Obsidian Sync ---
couchdb_image: "apache/couchdb:3.5.1"

# --- Kitsu (animation/VFX production tracker) ---
kitsu_image: "cgwire/cgwire:1.0.24"  # Kitsu 1.0.24 (2026-04-16)

# --- Tunnel / VPN-only ---
# cloudflared: RETIRÉ — remplacé par webhook relay via Seko-VPN (role webhook-relay)
# Les webhooks Meta passent maintenant par hook.<domain> (Seko-VPN) -> VPN mesh -> VPS
# cloudflared_image: "cloudflare/cloudflared:2025.1.0"

# --- Firefly III (personal finance engine) ---
firefly_image: "fireflyiii/core:version-6.5.3"

# --- Zimboo Dashboard (Next.js financial cockpit) ---
zimboo_image: "ghcr.io/mobutoo/zimboo:v1.14.0"

# --- Food & Stock (Koodia) ---
mealie_image: "ghcr.io/mealie-recipes/mealie:v3.15.1"  # 2026-04-14
grocy_image: "lscr.io/linuxserver/grocy:4.6.0"
koodia_image: "ghcr.io/mobutoo/koodia:v0.1.0"

# --- MeTube (video downloader — Workstation Pi) ---
metube_image: "alexta69/metube:2026.03.21"

# --- VideoRef Engine (video analysis pipeline — Workstation Pi) ---
videoref_image: "ghcr.io/mobutoo/videoref-engine:v0.1.0"

# --- Remotion Render Server (Workstation Pi — local build ARM64) ---
remotion_version: "4.0.437"

# --- Workstation Pi (natif — pas Docker) ---
# Caddy : binaire custom build via xcaddy (module dns.providers.ovh)
# Version alignee avec le prod Docker (caddy_image ci-dessus)
workstation_caddy_image: "caddy:2.11.2-alpine"  # reference version prod
# OpenCode : pinne apres premier deploy valide
# opencode_npm_version dans roles/opencode/defaults/main.yml
opencode_npm_version_pinned: "1.2.15"  # updated 2026-02-27

# Claude Code CLI : pinne apres premier deploy valide
# claude_code_npm_version dans roles/claude-code/defaults/main.yml
claude_code_npm_version_pinned: "2.1.62"  # updated 2026-02-27

# Codex CLI (OpenAI) : pinne apres premier deploy valide
# codex_cli_npm_version dans roles/codex-cli/defaults/main.yml
codex_cli_npm_version_pinned: "0.106.0"  # pinned 2026-02-27

# Gemini CLI (Google) : pinne apres premier deploy valide
# gemini_cli_npm_version dans roles/gemini-cli/defaults/main.yml
gemini_cli_npm_version_pinned: "0.30.0"  # updated 2026-02-27

# --- Design Tools (natif — pas Docker) ---
# OpenPencil : AI-native design editor, Figma alternative
# Version = git tag/branch, pin après premier deploy valide
openpencil_version_pinned: "master"  # TODO: pin to release tag after first deploy

# --- Penpot (VPS éphémère Hetzner CX23) ---
penpot_frontend_image: "penpotapp/frontend:2.6.1"
penpot_backend_image: "penpotapp/backend:2.6.1"
penpot_exporter_image: "penpotapp/exporter:2.6.1"
penpot_valkey_image: "valkey/valkey:8.1-bookworm"
penpot_pg_image: "postgres:15-bookworm"

# --- MCP Servers Pi (Docker HTTP/SSE persistants) ---
# Convention : un port dédié par service MCP, documenté ici
# 3001 → n8n-mcp (documentation n8n nodes, SQLite embarqué, docs-only)
# 3002 → réservé futur MCP server
# 3150 → openpencil-mcp (design editor MCP)
# 4401 → penpot-mcp (Penpot design tool MCP)
n8n_mcp_image_version_pinned: "2.40.5"  # pinned 2026-03-23 (sans v — Docker metadata semver pattern)
n8n_mcp_port_pi: 3001

# --- Flash Suite (unified shell + gamification engine) ---
flash_suite_shell_image: "ghcr.io/mobutoo/flash-suite:v1.0.0"
flash_suite_caddy_image: "caddy:2.11.2-alpine"
flash_suite_postgres_image: "postgres:16-alpine"
flash_suite_ase_image: "ghcr.io/mobutoo/ase:suite-v1.0.0"
flash_suite_zimboo_image: "ghcr.io/mobutoo/zimboo:suite-v1.0.0"
flash_suite_koodia_image: "ghcr.io/mobutoo/koodia:suite-v1.0.0"

# --- GrapeJS (HTML visual editor — wizy.<domain>) ---
# Build: cd roles/grapesjs/files && docker buildx build --platform linux/amd64 -t ghcr.io/mobutoo/grapesjs-editor:v1.1.0 --push .
grapesjs_image: "ghcr.io/mobutoo/grapesjs-editor:v1.2.0"  # GrapeJS 0.21.13 + node:22.14-alpine + Express API + SSE + DOCX import + page breaks + 11 blocs MOP

# --- MOP Machinery (NOC procedure generation) ---
gotenberg_image: "gotenberg/gotenberg:8.30.1"
msg2md_python_base: "python:3.12.10-slim"
carbone_image: "carbone/carbone-ee:full-4.26.3"
typebot_builder_image: "baptistearno/typebot-builder:3.16.1"
typebot_viewer_image: "baptistearno/typebot-viewer:3.16.1"
# Fake SMTP sidecar for Typebot magic-link login (no real email, local MailHog UI)
mailhog_image: "mailhog/mailhog:v1.0.1"
