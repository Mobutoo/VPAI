---
phase: 09-integration-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - roles/kitsu/templates/event_handler.py.j2
  - roles/n8n-provision/templates/workflows/creative-pipeline.json.j2
  - roles/n8n/templates/n8n.env.j2
  - roles/n8n-provision/files/workflows/cf-rough-cut.json
  - roles/n8n-provision/templates/workflows/cf-kitsu-sync.json.j2
  - inventory/group_vars/all/secrets.yml
autonomous: false
requirements: [FLOW-02, FLOW-05, FLOW-07]
gap_closure: true

must_haves:
  truths:
    - "event_handler.py.j2 references n8n_webhook_secret (not n8n_webhook_hmac_secret)"
    - "creative-pipeline.json.j2 calls /renders with compositionId field and reads jobId response"
    - "n8n.env.j2 exposes REMOTION_API_KEY and BYTEPLUS_API_KEY"
    - "cf-rough-cut.json sends action field in cf-update-content call body"
    - "cf-kitsu-sync.json.j2 STATUS_MAP includes locked->Approved mapping"
    - "Vault contains correct kitsu admin email and bot credentials"
  artifacts:
    - path: "roles/kitsu/templates/event_handler.py.j2"
      provides: "Correct webhook secret variable for Kitsu->n8n events"
      contains: "n8n_webhook_secret"
    - path: "roles/n8n-provision/templates/workflows/creative-pipeline.json.j2"
      provides: "Correct Remotion API integration"
      contains: "compositionId"
    - path: "roles/n8n/templates/n8n.env.j2"
      provides: "Complete env vars for n8n container"
      contains: "REMOTION_API_KEY"
    - path: "roles/n8n-provision/files/workflows/cf-rough-cut.json"
      provides: "Action-routed update call"
      contains: "update_content"
    - path: "roles/n8n-provision/templates/workflows/cf-kitsu-sync.json.j2"
      provides: "Complete status mapping"
      contains: "locked"
  key_links:
    - from: "roles/kitsu/templates/event_handler.py.j2"
      to: "inventory/group_vars/all/main.yml"
      via: "Jinja2 variable n8n_webhook_secret"
      pattern: "n8n_webhook_secret"
    - from: "roles/n8n-provision/templates/workflows/creative-pipeline.json.j2"
      to: "roles/remotion/files/server/index.ts"
      via: "HTTP POST /renders with compositionId"
      pattern: "/renders.*compositionId"
    - from: "roles/n8n/templates/n8n.env.j2"
      to: "roles/n8n-provision/templates/workflows/creative-pipeline.json.j2"
      via: "env var REMOTION_API_KEY consumed as $env.REMOTION_API_KEY"
      pattern: "REMOTION_API_KEY"
---

<objective>
Fix all template variable mismatches, n8n workflow field bugs, and vault credential errors that block E2E pipeline flows.

Purpose: These 5 template/workflow fixes and 1 vault fix close the 6 non-runtime integration issues from the v2026.3 audit. Without them, Kitsu events never reach n8n, Remotion renders fail, and cf-rough-cut updates are silently rejected.
Output: 6 corrected files ready for deployment in Plan 02.
</objective>

<execution_context>
@/home/mobuone/.claude/get-shit-done/workflows/execute-plan.md
@/home/mobuone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-integration-fixes/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Ansible template variable mismatches and add missing env vars</name>
  <files>roles/kitsu/templates/event_handler.py.j2, roles/n8n/templates/n8n.env.j2</files>
  <read_first>
    - roles/kitsu/templates/event_handler.py.j2
    - roles/n8n/templates/n8n.env.j2
    - inventory/group_vars/all/main.yml
  </read_first>
  <action>
1. In `roles/kitsu/templates/event_handler.py.j2`, line 11: change `{{ n8n_webhook_hmac_secret }}` to `{{ n8n_webhook_secret }}`. The variable `n8n_webhook_secret` is defined at line 172 of `inventory/group_vars/all/main.yml`. The current template uses a non-existent variable `n8n_webhook_hmac_secret` which resolves to empty string, causing all HMAC validation to fail silently.

2. In `roles/n8n/templates/n8n.env.j2`, add the following two blocks after the Kitsu section (after line ~196):

```
# Remotion API key — used by creative-pipeline workflow for authenticated renders
{% if vault_remotion_api_token | default('') | length > 0 %}
REMOTION_API_KEY={{ vault_remotion_api_token }}
{% endif %}

# BytePlus API key — used by creative-pipeline workflow for Seedance video generation
{% if byteplus_api_key | default('') | length > 0 %}
BYTEPLUS_API_KEY={{ byteplus_api_key }}
{% endif %}
```

The workflow reads `$env.REMOTION_API_KEY` but vault stores `vault_remotion_api_token` -- the env var name must match the workflow expectation. The `byteplus_api_key` variable is defined at line 130 of `main.yml`.
  </action>
  <verify>
    <automated>cd /home/mobuone/VPAI && grep -n 'n8n_webhook_secret' roles/kitsu/templates/event_handler.py.j2 | grep -v 'hmac' && grep -c 'REMOTION_API_KEY' roles/n8n/templates/n8n.env.j2 && grep -c 'BYTEPLUS_API_KEY' roles/n8n/templates/n8n.env.j2</automated>
  </verify>
  <acceptance_criteria>
    - event_handler.py.j2 contains `{{ n8n_webhook_secret }}` (NOT `n8n_webhook_hmac_secret`)
    - event_handler.py.j2 does NOT contain the string `n8n_webhook_hmac_secret`
    - n8n.env.j2 contains `REMOTION_API_KEY={{ vault_remotion_api_token }}`
    - n8n.env.j2 contains `BYTEPLUS_API_KEY={{ byteplus_api_key }}`
  </acceptance_criteria>
  <done>SC-1 and SC-3 resolved: event_handler uses correct variable, n8n env includes both missing API keys</done>
</task>

<task type="auto">
  <name>Task 2: Fix n8n workflow field and endpoint bugs</name>
  <files>roles/n8n-provision/templates/workflows/creative-pipeline.json.j2, roles/n8n-provision/files/workflows/cf-rough-cut.json, roles/n8n-provision/templates/workflows/cf-kitsu-sync.json.j2</files>
  <read_first>
    - roles/n8n-provision/templates/workflows/creative-pipeline.json.j2
    - roles/n8n-provision/files/workflows/cf-rough-cut.json
    - roles/n8n-provision/templates/workflows/cf-kitsu-sync.json.j2
    - roles/remotion/files/server/index.ts
  </read_first>
  <action>
1. In `roles/n8n-provision/templates/workflows/creative-pipeline.json.j2`, make THREE fixes per Remotion server API contract:
   - Change endpoint path from `/render` to `/renders` (the Remotion server registers `app.post("/renders", ...)` at line 85 of server/index.ts)
   - Change request field name from `composition` to `compositionId` (matches `RenderRequestSchema` in server/index.ts line 29: `compositionId: z.string()`)
   - Change response field reference from `input.renderId` to `input.jobId` in the Normalize output node (server returns `{ jobId: "..." }` at line 112)
   All three must be fixed together -- fixing only one still breaks the render chain.

2. In `roles/n8n-provision/files/workflows/cf-rough-cut.json`, find the Store Rough Cut URL node body (~line 60) and add `action: 'update_content'` field to the body object. Current body has `secret`, `content_id`, `rough_cut_url`, `current_step`, `status`. Add `action: 'update_content'` after `secret` so the cf-update-content webhook can route the request correctly (it uses action-based routing per Phase 8 design).

3. In `roles/n8n-provision/templates/workflows/cf-kitsu-sync.json.j2`, find the STATUS_MAP object in the Update Task Status node and add `'locked': 'Approved'` entry. Current map has: wip->WIP, done->Done, retake->Retake, invalidated->Retake, generating->Generating, todo->Todo. Add the locked entry after the last existing entry. "Approved" is a standard Zou task status created by `zou init-data`.
  </action>
  <verify>
    <automated>cd /home/mobuone/VPAI && grep -c '/renders' roles/n8n-provision/templates/workflows/creative-pipeline.json.j2 && grep -c 'compositionId' roles/n8n-provision/templates/workflows/creative-pipeline.json.j2 && grep -c 'jobId' roles/n8n-provision/templates/workflows/creative-pipeline.json.j2 && grep -c 'update_content' roles/n8n-provision/files/workflows/cf-rough-cut.json && grep -c "'locked'" roles/n8n-provision/templates/workflows/cf-kitsu-sync.json.j2</automated>
  </verify>
  <acceptance_criteria>
    - creative-pipeline.json.j2 contains `/renders` (NOT `/render"` without s)
    - creative-pipeline.json.j2 contains `compositionId` (NOT `composition` alone as field name)
    - creative-pipeline.json.j2 contains `input.jobId` (NOT `input.renderId`)
    - cf-rough-cut.json body contains `action` field with value `update_content`
    - cf-kitsu-sync.json.j2 STATUS_MAP contains `'locked':` key with `'Approved'` value
  </acceptance_criteria>
  <done>SC-2, SC-4, SC-5 resolved: Remotion API calls match server contract, rough-cut sends action field, status map covers locked state</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Update Ansible Vault credentials</name>
  <files>inventory/group_vars/all/secrets.yml</files>
  <action>
User must manually edit the Ansible Vault to update Kitsu admin email and add bot credentials. Claude cannot decrypt the vault.

1. Run `ansible-vault edit inventory/group_vars/all/secrets.yml`
2. Set `vault_kitsu_admin_email: "seko.mobutoo@gmail.com"`
3. Verify `vault_kitsu_admin_password` is set to the actual admin password
4. Add `vault_kitsu_bot_email: "javisi.bot@gmail.com"` if missing
5. Add `vault_kitsu_bot_password: "<secure-password>"` if missing
  </action>
  <verify>
    <automated>cd /home/mobuone/VPAI && source .venv/bin/activate && ansible-vault view inventory/group_vars/all/secrets.yml 2>/dev/null | grep -c 'kitsu_bot_email'</automated>
  </verify>
  <acceptance_criteria>
    - `ansible-vault view inventory/group_vars/all/secrets.yml | grep kitsu_admin_email` shows `seko.mobutoo@gmail.com`
    - `ansible-vault view inventory/group_vars/all/secrets.yml | grep kitsu_bot_email` shows `javisi.bot@gmail.com`
  </acceptance_criteria>
  <done>SC-8 resolved: Vault credentials match actual Kitsu server state, bot credentials exist for provisioning</done>
</task>

</tasks>

<verification>
All 6 template/workflow/vault fixes applied:
- `grep -rn 'n8n_webhook_hmac_secret' roles/` returns 0 results
- `grep -c 'compositionId' roles/n8n-provision/templates/workflows/creative-pipeline.json.j2` returns >= 1
- `grep -c 'REMOTION_API_KEY' roles/n8n/templates/n8n.env.j2` returns >= 1
- `grep -c 'update_content' roles/n8n-provision/files/workflows/cf-rough-cut.json` returns >= 1
- `grep -c 'locked' roles/n8n-provision/templates/workflows/cf-kitsu-sync.json.j2` returns >= 1
</verification>

<success_criteria>
- SC-1: event_handler.py.j2 uses `n8n_webhook_secret` variable
- SC-2: creative-pipeline.json.j2 calls `/renders` with `compositionId` and reads `jobId`
- SC-3: n8n.env.j2 includes `REMOTION_API_KEY` and `BYTEPLUS_API_KEY`
- SC-4: cf-rough-cut.json includes `action: 'update_content'` in body
- SC-5: cf-kitsu-sync.json.j2 STATUS_MAP includes `'locked': 'Approved'`
- SC-8: Vault credentials updated for admin and bot accounts
</success_criteria>

<output>
After completion, create `.planning/phases/09-integration-fixes/09-01-SUMMARY.md`
</output>
