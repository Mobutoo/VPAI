---
# .github/workflows/kaneo-build.yml
#
# OBSOLETE — Ce workflow ne build plus rien.
#
# La build des images Kaneo est maintenant gérée DIRECTEMENT dans le fork :
#   https://github.com/Mobutoo/kaneo/actions/workflows/ci-build.yml
#
# Images produites par le fork CI :
#   ghcr.io/mobutoo/api:<sha>
#   ghcr.io/mobutoo/web:<sha>
#
# Pour mettre à jour les images dans VPAI :
#   1. Merge/push vers main dans Mobutoo/kaneo
#   2. Récupérer le SHA du run CI
#   3. Mettre à jour inventory/group_vars/all/versions.yml :
#        kaneo_api_image: "ghcr.io/mobutoo/api:sha-<sha>"
#        kaneo_web_image: "ghcr.io/mobutoo/web:sha-<sha>"
#   4. make deploy-role ROLE=docker-stack ENV=prod

name: Kaneo — (Obsolete — See Mobutoo/kaneo fork)

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-24.04
    steps:
      - name: Info
        run: |
          echo "Images Kaneo sont buildées dans le fork Mobutoo/kaneo"
          echo "Voir : https://github.com/Mobutoo/kaneo/actions/workflows/ci-build.yml"
          echo ""
          echo "Ce workflow VPAI est obsolète."
