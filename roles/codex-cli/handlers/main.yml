---
# codex-cli — handlers
# Codex CLI is a CLI tool (not a daemon), no service to restart.
# Handlers are here as placeholders for future extensions.

- name: Reload codex-cli config
  ansible.builtin.debug:
    msg: "Codex CLI config updated — no restart needed (stateless CLI)."
  changed_when: false
